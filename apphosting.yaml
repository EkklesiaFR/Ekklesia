# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

runConfig:
  # Valeur maintenue à 1 pour limiter les coûts et stabiliser le trafic
  maxInstances: 1

# Note: Ce changement mineur est destiné à déclencher une nouvelle révision 
# pour tenter de résoudre le conflit de tags de trafic dans Cloud Run.
