rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }

    function getMemberData() {
      return get(/databases/$(database)/documents/members/$(request.auth.uid)).data;
    }

    function isAdmin() {
      return isSignedIn() 
        && exists(/databases/$(database)/documents/members/$(request.auth.uid))
        && getMemberData().role == "admin";
    }

    function isMemberActive() {
      return isSignedIn()
        && exists(/databases/$(database)/documents/members/$(request.auth.uid))
        && getMemberData().status == "active";
    }

    function isAdminActive() {
      return isMemberActive() && getMemberData().role == "admin";
    }

    match /members/{uid} {
      allow get: if isOwner(uid) || isAdmin();
      allow list: if isAdmin();
      
      allow create: if isOwner(uid) 
        && request.resource.data.status == "pending" 
        && request.resource.data.role == "member";
        
      allow update: if isAdmin() 
        || (isOwner(uid) 
            && !request.resource.data.diff(resource.data).affectedKeys().hasAny(['role', 'status', 'createdAt', 'id'])
           );
           
      allow delete: if isAdmin();
    }

    match /projects/{projectId} {
      allow read: if isMemberActive();
      allow write: if isAdminActive();
    }

    match /assemblies/{assemblyId} {
      allow read: if isMemberActive();
      allow write: if isAdminActive();

      match /votes/{voteId} {
        allow read: if isMemberActive();
        
        // Autorise les admins et les membres (uniquement pour l'incrémentation du ballotCount lors du premier vote)
        allow update: if (isAdminActive() 
                        && request.resource.data.diff(resource.data).affectedKeys().hasOnly(['state', 'results', 'closedAt', 'updatedAt', 'publishedAt', 'lockedAt', 'ballotCount', 'eligibleCount', 'openedAt']))
                      || (isMemberActive()
                          && resource.data.state == "open"
                          && request.resource.data.diff(resource.data).affectedKeys().hasOnly(['ballotCount', 'updatedAt'])
                          && request.resource.data.ballotCount == (resource.data.ballotCount == null ? 1 : resource.data.ballotCount + 1)
                          && !exists(/databases/$(database)/documents/assemblies/$(assemblyId)/votes/$(voteId)/ballots/$(request.auth.uid))
                          && existsAfter(/databases/$(database)/documents/assemblies/$(assemblyId)/votes/$(voteId)/ballots/$(request.auth.uid)));

        allow create: if isAdminActive();
        allow delete: if isAdminActive();

        match /ballots/{uid} {
          allow get: if isOwner(uid) || isAdminActive();
          allow list: if isAdminActive(); 

          function isValidBallotRequest() {
            let vote = get(/databases/$(database)/documents/assemblies/$(assemblyId)/votes/$(voteId)).data;
            return isMemberActive()
              && uid == request.auth.uid
              && vote.state == "open"
              && request.resource.data.keys().hasOnly(['ranking', 'castAt', 'updatedAt']);
          }

          allow create: if isValidBallotRequest();
          allow update: if isValidBallotRequest();
          allow delete: if false;
        }
      }
    }
  }
}